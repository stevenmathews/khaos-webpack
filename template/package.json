{
  "name": "{{basename}}",
  "description": "{{description}}",
  "version": "0.0.0",
  "author": "{{author}}",
  "bugs": {
    "url": "{{repository}}/issues"
  },
  "homepage": "{{repository}}#readme",
  "license": "MIT",
  "main": "app.js",
  "repository": {
    "type": "git",
    "url": "git+{{repository}}.git"
  },
  "scripts": {
    "start": "NODE_ENV=dev webpack-dev-server --inline --quiet",
    "build": "NODE_ENV=production webpack",
    "test": "standard && echo \"Error: no test specified\" && exit 1",
    "deploy": "surge -p public -d {{basename}}.surge.sh",
  },
  "devDependencies": {
    "babel-core": "^6.7.2",
    "babel-eslint": "^6.0.4",
    "babel-loader": "^6.2.4",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "browser-sync": "^2.12.8",
    "browser-sync-webpack-plugin": "^1.0.1",
    "hjs-webpack": "^8.2.0",
    "standard": "^7.1.0",
    "surge": "^0.17.7",
    "webpack": "^1.12.14",
    "webpack-dev-server": "^1.14.1"
  },
  "standard": {
    "parser": "babel-eslint"
  }
}